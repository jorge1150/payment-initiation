package com.hiberus.payment_initiation.shared;

/**
 * Shared / Common Layer
 * <p>
 * This package contains cross-cutting concerns and utilities that can be
 * used across all layers. It may include common exceptions, constants,
 * utility classes, and shared value objects that don't belong to a specific
 * domain context.
 */
public class SharedPlaceholder {
    // Placeholder class to ensure package structure
}

