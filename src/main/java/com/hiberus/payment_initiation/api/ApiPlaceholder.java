package com.hiberus.payment_initiation.api;

/**
 * API Layer (Hexagonal Architecture - Adapters - Inbound)
 * <p>
 * This package contains the REST controllers and API endpoints that expose
 * the application's functionality to external clients. These are the primary
 * entry points for HTTP requests and handle request/response mapping.
 * <p>
 * Controllers in this layer should be thin and delegate business logic to
 * the application layer.
 */
public class ApiPlaceholder {
    // Placeholder class to ensure package structure
}

